{
  "name": "burnglass",
  "version": "2.0.1",
  "description": "A local, zero-dependency usage dashboard for Claude Code, OpenAI Codex and other coding agents. The server only reads the agents' local logs and has zero runtime dependencies; the React frontend (web/) is built to static files it serves. Runs on http://localhost:4747.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "burnglass": "server.js",
    "pulse": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix web install && npm --prefix web run build",
    "dev": "npm --prefix web run dev",
    "make-exe": "node build/make-exe.mjs",
    "inspect-schema": "node server.js --inspect-schema"
  },
  "keywords": [
    "claude",
    "claude-code",
    "usage",
    "dashboard",
    "monitor",
    "local",
    "zero-dependency"
  ]
}
