# Runtime / install artifacts
burnglass.log
pulse.log
node_modules/
npm-debug.log*
.DS_Store

# single-executable build outputs
dist-exe/
dist-installer/
build/sea-prep.blob
build/sea-config.json

# Burnglass Strip (strip/) — C# build outputs. Only the SOURCE is committed; the
# exe is built by CI and shipped as a release asset (dist-strip/burnglass-strip.exe
# alone is ~105 MB, so this must never reach git history).
strip/bin/
strip/obj/
strip/dist-strip/
strip/shots/

# Claude Code session tooling (isolated agent worktrees) — never commit
.claude/worktrees/
